server:
  port: 8761

spring:
  application:
    name: EurekaServer

eureka:
  client:
    register-with-eureka: false     # Eureka 서버에 자신을 등록하지 않음
    fetch-registry: false           # 레지스토리 정보를 로컬에 캐싱하지 않음
